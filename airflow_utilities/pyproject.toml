[project]
name = "airflow-utilities"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["airflow_utilities"]

[tool.setuptools.package-dir]
airflow_utilities = "."

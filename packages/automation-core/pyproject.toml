[project]
name = "automation-core"
version = "0.1.0"
requires-python = ">=3.14,<3.15"
dependencies = ["hvac", "niquests"]

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"

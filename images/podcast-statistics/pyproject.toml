[project]
name = "automation-podcast-statistics"
version = "0.1.0"
requires-python = ">=3.14,<3.15"
dependencies = ["automation", "podcast-statistics"]

[tool.uv]
package = false

[tool.uv.sources]
automation = { workspace = true }
podcast-statistics = { workspace = true }

[project]
name = "automation-download-zanzara"
version = "0.1.0"
requires-python = ">=3.14,<3.15"
dependencies = ["automation", "download-zanzara"]

[tool.uv]
package = false

[tool.uv.sources]
automation = { workspace = true }
download-zanzara = { workspace = true }

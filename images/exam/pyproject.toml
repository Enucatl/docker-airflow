[project]
name = "automation-exam"
version = "0.1.0"
requires-python = ">=3.14,<3.15"
dependencies = ["automation-core", "playwright", "psycopg[binary]"]

[tool.uv]
package = false

[tool.uv.sources]
automation-core = { path = "../../packages/automation-core" }

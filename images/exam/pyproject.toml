[project]
name = "automation-exam"
version = "0.1.0"
requires-python = ">=3.14,<3.15"
dependencies = ["automation", "exam"]

[tool.uv]
package = false

[tool.uv.sources]
automation = { workspace = true }
exam = { workspace = true }

[project]
name = "automation-cyber-analyst"
version = "0.1.0"
requires-python = ">=3.14,<3.15"
dependencies = ["automation", "cyber-analyst"]

[tool.uv]
package = false

[tool.uv.sources]
automation = { workspace = true }
cyber-analyst = { workspace = true }

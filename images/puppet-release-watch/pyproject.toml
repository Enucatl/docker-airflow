[project]
name = "automation-puppet-release-watch"
version = "0.1.0"
requires-python = ">=3.14,<3.15"
dependencies = ["automation", "puppet-release-watch"]

[tool.uv]
package = false

[tool.uv.sources]
automation = { workspace = true }
puppet-release-watch = { workspace = true }
